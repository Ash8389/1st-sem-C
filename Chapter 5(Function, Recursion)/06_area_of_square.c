#include<stdio.h>
#include<math.h>

int main()
{
    int a;
    printf("Enter the length of square\n");
    scanf("%d", &a);
    printf("The area of square is %f", pow(a,2));

    return 0;
}