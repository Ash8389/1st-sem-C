#include <stdio.h>
#include <string.h>

int main()
{
    // char *str = "Ashish";
    char str[] = "Ashish";

    int a = strlen(str);

    printf("Lenghth of the string is : %d", a);

    return 0;
}